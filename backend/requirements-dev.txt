# Local development and CI (install with: pip install -r requirements.txt -r requirements-dev.txt)
pytest==8.3.4
# Starlette 0.27 TestClient is incompatible with httpx 0.28+ (ASGITransport API)
httpx==0.27.2
ruff==0.8.6
